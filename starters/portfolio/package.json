{
  "name": "@extora/portfolio",
  "version": "0.0.0",
  "private": true,
  "description": "Extora portfolio — pending implementation",
  "scripts": {
    "build": "echo 'No build yet'",
    "clean": "rm -rf dist"
  }
}
