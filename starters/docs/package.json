{
  "name": "@extora/docs",
  "version": "0.0.0",
  "private": true,
  "description": "Extora docs — pending implementation",
  "scripts": {
    "build": "echo 'No build yet'",
    "clean": "rm -rf dist"
  }
}
