{
  "name": "@extora/config",
  "version": "0.0.0",
  "private": true,
  "description": "Extora config — pending implementation",
  "main": "./src/index.ts",
  "dependencies": {
    "@extora/types": "workspace:*"
  },
  "scripts": {
    "build": "echo 'No build yet'",
    "typecheck": "echo 'No typecheck yet'",
    "lint": "echo 'No lint yet'",
    "clean": "rm -rf dist"
  }
}
